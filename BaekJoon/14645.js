// const input = require("fs").readFileSync("testInput.txt").toString().split("\n").map(v => v.split(" ").map(Number));

// for(let i = 0; i < input.length; i++) {
//     if(i === input.length-1)console.log('비와이');
// };
console.log('비와이');